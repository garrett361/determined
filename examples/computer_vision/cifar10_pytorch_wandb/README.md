Slightly modified version of our `cifar10_pytorch` example to demonstrate `wandb`.

To use:

- Create a Weights and Biases account and a `determined` project.
- Copy your `wandb` api key into the `startup-hook.sh`'s `WANDB_API_KEY` line.
